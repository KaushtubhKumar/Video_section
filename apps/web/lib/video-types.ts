// Re-exported from the shared package so both apps use one source of truth for the Video type.
export { BLUR_DATA_URL, formatDuration, formatViews, formatRelativeDate } from "@video-module/shared";
export type { Video } from "@video-module/shared";
